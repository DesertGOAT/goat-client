// Package main is the goat-client desktop GUI binary (Fyne).
//
// Talks to the goat-clientd daemon via Unix-socket / named-pipe IPC.
// Surfaces tunnel status, bundle import, diagnostics, system tray.
package main

import "fmt"

func main() {
	fmt.Println("goat-client GUI: scaffolding stub — see HANDOFF.md for parallel build-out tracks")
}
