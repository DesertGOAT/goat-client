# goat-client
Cross-platform daemon + GUI for goat wg-cp0 silent control plane onboarding (Linux/macOS/Windows desktop + iOS/Android mobile). Apache 2.0. Forks netbird's MIT client/ tree, strips mesh-specific logic, uses offline-CA-signed CBOR bundle for onboarding.
